# 一个py文件就是一个模块，不同模块函数名和变量名不冲突，但是要避免内置函数名字冲突
#python也有包概念，来避免模块命名冲突。 我觉得模块就是java中的类了。 每个包下都有_init_.py文件，不然会认为这是一个普通目录，无法计入包

#模块名要遵循Python变量命名规范，不要使用中文、特殊字符；


#import 导入模块    就是导入模块语法

#有时候只想导入某个模块中的某个方法 语法
# from math import floor     #导入math模块中的floor函数方法